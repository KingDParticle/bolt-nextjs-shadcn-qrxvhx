import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Cosmic Journey Within | Spiritual Awakening & Cosmic Consciousness',
  description: 'Embark on a spiritual journey to discover your cosmic heritage, explore sacred geometry, and access ancient wisdom through the Akashic Records.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}